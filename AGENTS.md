# Harness Self-Evolution Plugin - Agent 协同指南

## 概述

本文件定义了 Harness Self-Evolution 插件中各 Agent 的角色、职责和任务边界。
所有 Agent 协同工作，共同实现插件的自进化升级流水线。

> **设计契约参考**：详细的系统架构、组件设计和进化流程请参阅 [DESIGN.md](DESIGN.md)。

## Agent 角色与职责

### 1. Code Generator Agent (代码生成者)

**职责**：按已审批的进化提案实现具体代码变更

**能力范围**：
- 工具合并（如 `navigate + click + type → smart_fill`）
- 中间件实现（如循环检测、性能优化）
- 能力扩展（新增工具或功能）
- 错误处理改进

**设计原则**：
- 遵循「Deep Module > 浅模块」原则：接口窄、实现厚
- 合并工具时保住外部行为
- 加中间件时不侵入既有调用链
- 扩展能力时优先复用既有模块

**输入**：
- 一份进化提案（含 `evolution_type`、`proposed_changes`、`validation_plan`）
- 相关源码文件的路径

**输出**：
- 每个变更项独立落盘，改动范围最小化
- 错误处理与日志风格对齐目标文件的既有惯例
- 变更摘要：改了哪些文件、每个文件对应哪个变更项、有什么行为影响

**边界**：
- 不运行完整回归验证（那是 Validator 的职责），只做编辑器级的语法自查
- 不修改提案文档本身，也不更新文档文件（那是 Doc Writer 的职责）
- 破坏性变更（改函数签名、删工具）必须在变更摘要里显著标注

---

### 2. Test Writer Agent (测试编写者)

**职责**：按提案的验证计划编写测试用例

**设计原则**：
- 测试是移植与升级正确性的客观参照
- 每条用例都必须先看见它失败（对旧代码），再看见它通过（对新代码）
- 测不出差异的测试是装饰

**输入**：
- 提案里的 `validation_plan`：测试场景清单、成功标准、回滚策略
- Code Generator 产出的变更摘要与相关源码

**输出**：
- 用例名使用第三人称行为描述（`generates ...` / `rejects ...`）
- 覆盖正常路径与至少一条边界路径（空输入、非法值、重复执行）
- 断言必须具体（比较确定的值或形状）

**边界**：
- 只新增或修改测试文件，不改业务代码
- 发现业务代码的疑似缺陷时写进报告，由主流程裁决
- 不为了让测试通过而放松断言或跳过用例
- 不运行完整回归套件（那是 Validator 的职责）

---

### 3. Doc Writer Agent (文档编写者)

**职责**：按 `Change::UpdateDocumentation` 变更项同步更新插件文档

**设计原则**：
- 「词汇即文档」：文档里的每个术语、每个取值、每条行为描述都必须与代码单一事实来源一致
- 文档描述不存在的能力比不描述更糟——它会让读者去依赖永远不会发生的行为

**输入**：
- 提案中的 `Change::UpdateDocumentation` 变更项（指明目标文件与意图）
- Code Generator 的变更摘要（行为是否变化的权威依据）

**输出**：
- 只改提案点名的文档文件
- 数字与清单类内容（工具数量、用例数、默认值）更新后逐字核对
- 新概念先登记进词汇表再使用
- 改动清单：文件、位置、改前/改后的关键句

**边界**：
- 只动文档（*.md），绝不碰源码、测试或配置
- 发现代码与文档冲突且提案未覆盖时，写进报告交回主流程
- 不顺手重写与变更项无关的章节
- 不虚构行为：写不准的内容宁可标注「待确认」，也不编造

---

### 4. Integration Agent (集成协调者)

**职责**：处理进化提案的依赖关系与兼容性

**设计原则**：
- 插件生态里的破坏往往是间接的：改一个函数签名会打断下游调用方，换一个依赖版本会动摇可复现构建
- 在变更合入前把这些坑找出来

**输入**：
- 提案与风险评估（`backward_compatible`、`migration_effort`、`breaking_changes`）
- Code Generator 的变更摘要与涉及文件的完整现状

**输出**：
- 逐个调用方核对：被改动的接口在仓库内（必要时含声明的外部契约）还有谁在用
- 依赖变化必须核对锁定方式（精确版本、锁文件），禁止引入未声明的间接依赖
- 兼容性结论：每个风险点、证据（文件与行）、是否阻塞合入

**边界**：
- 只读与执行查询类命令，不修改任何文件
- 调整代码是 Code Generator 的职责
- 结论必须带证据，不做「应该没问题」式的口头担保
- 发现破坏性影响时如实标注阻塞，不替提案粉饰

---

### 5. Validator Agent (验证者)

**职责**：执行 T0/T1/T2 三级验证并出具只读的验证结论

**设计原则**：
- 验证是「自动改代码失控」的最后一道闸门
- 结论只认证据：命令的退出码、断言的通过数、日志里的关键行，不认任何人的口头承诺

**三级验证**：
- **T0 语法验证**：静态检查零错零警（对 MoonBit 即 `moon check --deny-warn`）；任何新警告都算失败
- **T1 功能验证**：运行变更相关的测试套件，全绿才算过；失败时给出第一条失败用例名与断言差异
- **T2 回归验证**：运行全量构建与测试（含架构守卫），确认旧功能无一回归

**输入**：
- Code Generator 与 Test Writer 的产出清单（改动文件、新增用例）
- 每一级验证对应的命令与判定标准

**输出**：
- 逐级输出：执行的命令、退出码、关键输出摘录、结论
- 任一级失败立即停止并报告，不做「顺手修复」
- 结论只有三种：pass / fail（附第一证据）/ unable（环境不允许，说明缺什么）

**边界**：
- **只读不改**：不修改任何文件、不重跑生成、不为了绿灯放宽判定标准
- 不跳级：T0 不过不跑 T1
- 你的输出是合入决策的唯一验证依据，必须让第三方能凭你的记录复现结论

---

## Agent 协同工作流

### 进化提案执行流程

```
提案审批 → Executor 启动 → 任务分解 → Agent 协同 → 验证测试 → 部署上线
    │              │              │              │              │
    │              │              │              │              └─ 更新 Registry
    │              │              │              └─ Validator 三级验证
    │              │              └─ 按 DAG 分层并行执行
    │              └─ 分解任务 + 分配 Agent
    └─ 用户确认
```

### 任务依赖与并行执行

任务依赖用 **DAG（有向无环图）** 表达，由 `executor/dag.mbt` 做拓扑分层：
- **同层任务可并行执行**
- **层间任务串行依赖**

典型依赖关系：
```
Code Generator ─┬─ Test Writer ── Validator (T1)
                │
                ├─ Doc Writer
                │
                └─ Integration ── Validator (T2)
```

### 三级验证流程

1. **T0 语法验证**：Code Generator 完成后立即执行
2. **T1 功能验证**：Test Writer 完成后执行，验证新功能
3. **T2 回归验证**：所有 Agent 完成后执行，确认无回归

---

## 设计契约与约束

### 架构不变量

所有 Agent 必须遵守以下架构不变量（详见 [CONTEXT.md](CONTEXT.md)）：

1. **JSONL I/O 只存在于 `store/`** — 任何 Agent 不得直接读写数据文件
2. **提案状态只经 `ProposalStore` 流转** — 状态机约束必须严格遵守
3. **信号检测热路径不做超过 O(1) 的工作** — 性能约束
4. **MCP 工具处理器不写 try/catch** — 错误统一处理
5. **无清单目录不是插件** — 扫描约束
6. **stdout 只承载协议字节** — 日志约束
7. **一个字符串枚举只有一张 wire 表** — 词汇表约束
8. **包依赖图严格分层，每条边只向下** — 架构约束
9. **默认数据目录只有一处定义** — 配置约束

### Matt Pocock 原则

所有进化必须遵循 Matt Pocock 工程原则：

| 原则 | 进化策略 | 实现方式 |
|------|---------|---------|
| **先对齐，再动手** | 建立性能基线 | Scanner 初始扫描 + Monitor 持续跟踪 |
| **垂直切片** | 单插件独立进化 | 每个插件独立的进化路径 |
| **紧反馈环** | 快速验证循环 | 小步迭代 + 实时测试 |
| **Deep Module** | 简化接口 | 合并工具、优化参数、隐藏细节 |
| **架构扫描** | 全局优化 | 周期性跨插件分析 |
| **词汇即文档** | 进化记录即文档 | 自动更新 SKILL.md |

### 信号检测机制

**强信号**（立即触发进化）：
- 用户明确纠正插件行为
- 用户明确表达"以后都这样/不要这样"
- 连续失败 >= 3 次
- 性能指标持续下降（> 20%）

**中信号**（累积触发）：
- 同类任务出现清晰模式
- 某个参数组合反复出错
- 用户偏好重复出现（>= 2 次）
- 循环行为被检测到

**弱信号**（仅记录）：
- 轻微性能波动
- 一次性上下文需求
- 用户明确说"这次只是临时的"

---

## 安全边界

### 只读扫描
Scanner 不修改任何插件代码，只建立初始档案。

### 审批强制
所有进化必须经 `approve_proposal` 工具确认，人工审批是「自动改代码失控」的唯一闸门。

### 回滚保留
原版本保留 30 天，确保可以回退。

### 沙箱测试
升级在隔离环境验证，三级验证确保质量。

### 权限最小
仅访问必要目录和事件，遵循最小权限原则。

---

## 参考文档

- [DESIGN.md](DESIGN.md) — 详细设计、模块边界、调用链
- [CONTEXT.md](CONTEXT.md) — 设计上下文、缺陷清单、配置来源、架构守卫
- [README.md](README.md) — 项目概述、安装配置、使用说明
- [docs/subagent-factory.md](docs/subagent-factory.md) — 子 Agent 工厂设计
- [DSH_INTEGRATION.md](DSH_INTEGRATION.md) — DSH Sub-Agent 集成指南

---

## 版本信息

- **当前版本**：3.0.0
- **最后更新**：2026-09-20
- **维护者**：AI Agent Designer
- **许可证**：MIT