# 交付验收流程与恢复程序

## 1. 交付验收流程

### 1.1 验收标准
所有交付必须满足以下条件：

1. **代码质量检查**：`.\build.ps1 -Task check` 通过（无警告）
2. **单元测试**：`.\build.ps1 -Task test` 通过（所有测试用例通过）
3. **构建成功**：`.\build.ps1 -Task build` 成功生成 `bin\harness-evolution.exe`
4. **集成测试**：在实际 Harness 环境中验证插件功能
5. **文档更新**：相关文档已更新（如有必要）

### 1.2 验收流程
1. 开发者完成功能开发后，运行完整构建流程：
   ```powershell
   .\build.ps1 -Task all
   ```
2. 检查构建输出，确保无错误
3. 运行集成测试（如适用）
4. 更新版本号（在 `moon.mod` 中）
5. 提交代码并推送至仓库
6. 创建 Pull Request（如适用）
7. 代码审查通过后合并

## 2. 恢复程序

### 2.1 恢复场景
1. **构建失败**：代码检查或测试失败
2. **功能回退**：新版本引入了回归问题
3. **部署问题**：插件在生产环境中出现问题

### 2.2 恢复步骤

#### 2.2.1 构建失败恢复
1. 检查构建日志，定位失败原因
2. 修复代码问题
3. 重新运行构建流程：
   ```powershell
   .\build.ps1 -Task all
   ```
4. 验证修复后构建成功

#### 2.2.2 功能回退恢复
1. 识别引入问题的提交（使用 `git bisect` 或手动检查）
2. 回滚到上一个已知良好的提交：
   ```bash
   git revert <commit-hash>
   # 或
   git reset --hard <previous-commit>
   ```
3. 重新构建并测试
4. 部署回滚版本

#### 2.2.3 部署问题恢复
1. 立即回滚到上一个稳定版本
2. 收集问题日志和诊断信息
3. 在开发环境中重现问题
4. 修复问题并重新走交付流程

### 2.3 恢复验证
1. 确保回滚版本通过所有检查：
   ```powershell
   .\build.ps1 -Task all
   ```
2. 在测试环境中验证功能
3. 确认问题已解决

## 3. 责任分工

### 3.1 交付验收
- **主要责任人**：项目维护者
- **审查人员**：代码审查者
- **最终批准**：项目负责人

### 3.2 恢复程序
- **执行人**：任何发现的开发者
- **协调人**：项目维护者
- **验证人**：测试人员

## 4. 沟通流程

1. **问题发现**：立即通知项目维护者
2. **状态更新**：每2小时更新恢复进展
3. **解决确认**：问题解决后通知所有相关方

## 5. 预防措施

1. **代码审查**：所有代码变更必须经过审查
2. **自动化测试**：持续集成运行所有测试
3. **版本控制**：使用语义化版本控制
4. **监控告警**：生产环境监控关键指标

## 6. 文档维护

本文档应随项目发展定期更新，至少每季度审查一次。

---

**最后更新**：2026-09-09
**文档负责人**：项目维护者