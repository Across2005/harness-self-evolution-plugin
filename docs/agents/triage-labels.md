# Triage Labels

各 skill 以五个规范 triage 角色说话。本文件把这些角色映射到本仓库 issue tracker 里实际使用的标签字符串。

| 角色（mattpocock/skills） | 本仓库 tracker 标签 | 含义 |
| ------------------------ | ------------------- | ---- |
| `needs-triage`           | `needs-triage`      | 维护者需评估此 issue |
| `needs-info`             | `needs-info`        | 等待报告者补充信息 |
| `ready-for-agent`        | `ready-for-agent`   | 已完整规格化，可交给 AFK agent |
| `ready-for-human`        | `ready-for-human`   | 需要人来实现 |
| `wontfix`                | `wontfix`           | 不会处理 |

skill 提到某个角色时（例如「apply the AFK-ready triage label」），使用上表对应的标签字符串。

若本仓库 tracker 已有其他命名，改右列以匹配既有词汇，避免 `/triage` 造出重复标签。
